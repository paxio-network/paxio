# M-L10.4 — Hero (HeroVariantB5 + preview.ts)

**Тип**: Frontend implementation (frontend-dev). Самая большая фаза.
**Зависимости**: M-L10.2 (CSS tokens) + M-L10.3 (Shell components) merged.
**Worktree**: `/tmp/paxio-ml10.4-hero`.
**Branch**: `feature/M-L10.4-hero`.
**Estimated**: 4-6 hours.

## Цель

Port `HeroVariantB5` из `docs/design/paxio-b5/components/v_hero_b5.jsx` в Next.js Client
Component. Hardcoded numbers вынести в `app/data/preview.ts` под R-FE-Preview rule
(`frontend-rules.md::R-FE-Preview`) с обязательными `// TODO M-LN: replace with real API`
маркерами.

## Готово когда

1. `apps/frontend/landing/app/data/preview.ts` — frozen exports:
   - `PREVIEW_AGENTS` — 16 агентов по shape `AgentListItem` из `@paxio/types`
     (mapping из `v_hero_b5.jsx::AGENTS` array). Marker: `// TODO M-L11: replace with paxioClient.registry.list(...)`
   - `PREVIEW_TICKER_INITIAL` — initial state для `useTicker` (PAEI/btc/legal/finance/research/cx/walletAdoption/x402Share/btcShare/hhi/drift7/attacks24/slaP50/fapThroughput/uptimeAvg/agents/txns + delta fields). Marker: `// TODO M-L11: replace with paxioClient.intelligence.getPaeiSnapshot(...)`
   - `PREVIEW_MOVERS` — top gainers/losers + 90-day paeiHistory из `v_hero_b5.jsx`. Marker: `// TODO M-L11: replace with paxioClient.intelligence.getMovers(...)`
   - `Object.freeze` на всех exports
2. `apps/frontend/landing/app/sections/01-hero-b5.tsx` ('use client'):
   - Imports `PREVIEW_AGENTS`, `PREVIEW_TICKER_INITIAL`, `PREVIEW_MOVERS` из `@/app/data/preview`
   - Subcomponents (inline OR в `app/sections/_hero/`): `useTicker` hook, `Spark`, `fmtMoney`,
     `PaeiTicker`, `FilterBar`, `AgentTable`, `MarketMovers`
   - `useTicker` использует `Math.random()` под R-FE-Preview exception (legitimate под условия
     в `frontend-rules.md`)
   - Visual fidelity: side-by-side с `_design/Paxio-B5.html` rendered в browser
3. `pnpm --filter @paxio/landing-app build` clean
4. `pnpm --filter @paxio/landing-app test hero` GREEN

## RED test specs (architect-only)

`apps/frontend/landing/tests/hero-b5.test.tsx` (new):
- 16 PREVIEW_AGENTS items имеют все required fields из `ZodAgentListItem`
- All 3 preview exports `Object.isFrozen(...)` true
- 16+ `// TODO M-L11` маркеров в preview.ts (по grep)
- Hero rendered (jsdom) → ticker section visible, table renders 16 rows, market movers visible
- `useTicker` интервал чистится при unmount (no leak)

`scripts/verify_M-L10.4.sh` — idempotent acceptance.

## Промт для frontend-dev (~30 строк)

```
Ты frontend-dev. Реализуешь M-L10.4 — Hero.

SETUP:
  cd /home/nous/paxio
  git fetch origin
  git worktree add /tmp/paxio-ml10.4-hero -b feature/M-L10.4-hero origin/dev
  cd /tmp/paxio-ml10.4-hero
  git config user.name frontend-dev
  git config user.email frontend-dev@paxio.network
  pnpm install

ЧИТАТЬ:
  - docs/design/paxio-b5/EXTRACTED.md                       (sections "Live ticker", "Tables", "Sparkline")
  - docs/design/paxio-b5/components/v_hero_b5.jsx           (460 строк — единственный raw source который нужен)
  - docs/sprints/M-L10.4-hero.md                            (этот файл)
  - .claude/rules/frontend-rules.md                         (R-FE-Preview section — обязательные условия)
  - packages/types/src/intelligence.ts                      (ZodAgentListItem — shape для PREVIEW_AGENTS)
  - apps/frontend/landing/tests/hero-b5.test.tsx            (RED тесты)

ПИШЕШЬ:
  - apps/frontend/landing/app/data/preview.ts                       (PREVIEW_AGENTS + PREVIEW_TICKER_INITIAL + PREVIEW_MOVERS)
  - apps/frontend/landing/app/sections/01-hero-b5.tsx               ('use client', основная section)
  - apps/frontend/landing/app/sections/_hero/{Ticker,Table,Movers,Spark}.tsx   (опционально — split если файл > 300 lines)

ПРОВЕРЯЕШЬ:
  pnpm typecheck
  pnpm --filter @paxio/landing-app test hero-b5
  pnpm --filter @paxio/landing-app dev   → http://localhost:3001 → визуальный side-by-side с Paxio-B5.html

КОММИТЯТ один commit. "M-L10.4 готов".

Cleanup: git worktree remove --force /tmp/paxio-ml10.4-hero

НЕ читай: всю папку _design/styles/ (EXTRACTED.md заменяет), tech-debt полностью,
existing M-L9 sections (deprecated в M-L10.5).
```

## R-FE-Preview compliance

`Math.random()` в `useTicker` РАЗРЕШЁН в этом файле под exception потому что:
1. `<body data-production="false">` (будет установлено в M-L10.5 — пока не делай)
2. `<PreviewRibbon/>` будет visible (M-L10.3 + M-L10.5 wire)
3. ВСЕ simulated данные изолированы в `app/data/preview.ts`
4. Drift-guard test (Phase 0 review) проверит маркеры `// TODO M-L11`

Но НЕ:
- Auth simulated
- Money operations simulated
- Form submissions simulated

Для всего остального preview-mode разрешён.

## Анти-цели

- НЕ port'ить Scrolls (M-L10.5)
- НЕ удалять M-L9 sections — wire happens в M-L10.5
- НЕ забыть `Object.freeze` на preview exports (drift-guard fail)
- НЕ забыть TODO маркеры
- НЕ ставить `data-production="false"` или PreviewRibbon — это M-L10.5
