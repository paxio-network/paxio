# M-L10.3 — Shell components (@paxio/ui::Header / Footer / PreviewRibbon)

**Тип**: Frontend implementation (frontend-dev).
**Зависимости**: M-L10.2 (CSS tokens merged).
**Worktree**: `/tmp/paxio-ml10.3-shell`.
**Branch**: `feature/M-L10.3-shell`.
**Estimated**: 2 hours.

## Цель

Создать 3 переиспользуемых компонента в `packages/ui/src/`, экспортируемых из `@paxio/ui`.
Оба landing и Registry апп'а потом будут их использовать (визуальная унификация).

## Готово когда

1. `packages/ui/src/Header.tsx` ('use client'):
   - SVG mark (5-node graph + gold accent) идентично `_design/Paxio-B5.html` lines 31-49
   - Wordmark "Paxio" + tagline "Financial OS · Agentic Economy"
   - Nav: Registry, Radar, Docs (links)
   - Theme toggle button с light/dark icons (см. `Paxio-B5.html` lines 60-61)
   - 2 CTA: "Install SDK" outline + "Open Registry →" primary
   - Live pulse-dot + "live" label
   - Mobile menu trigger (hamburger) при `< 900px`
   - Mobile drawer с теми же links — через Radix Dialog
2. `packages/ui/src/Footer.tsx`:
   - Mark + tagline ("Financial OS for the agentic economy.")
   - 3 columns: Product (Registry/Wallet/Pay/Radar) / Builders (Docs/SDK/GitHub/Status) /
     Company (Manifesto/Blog/Careers/Contact)
   - Foot-legal с "© 2026 Paxio GmbH · Berlin" + projected-metrics disclaimer
   - Принимает `year?: number` prop (default `2026` — server-side computed по M-L9 fix)
3. `packages/ui/src/PreviewRibbon.tsx` ('use client'):
   - Sticky top, role="status", non-dismissable
   - Marquee animation 60s linear infinite
   - Текст: "SIMULATED PREVIEW · LAUNCHING Q2 2026 · METRICS ARE PROJECTED" повторённый
   - Honors `[data-motion="off"]` (animation paused для prefers-reduced-motion)
4. `packages/ui/src/index.tsx` экспортирует все 3 + типы props
5. RED tests в `packages/ui/tests/`:
   - `header.test.tsx` — рендерит, есть mark + 3 nav links + 2 CTA + theme toggle
   - `footer.test.tsx` — рендерит mark + 3 col headers + foot-legal
   - `preview-ribbon.test.tsx` — рендерит role="status" + marquee text + non-dismissable

## RED test specs (architect-only)

`packages/ui/tests/{header,footer,preview-ribbon}.test.tsx` (3 new files).
`scripts/verify_M-L10.3.sh` — idempotent acceptance.

## Промт для frontend-dev (~30 строк)

```
Ты frontend-dev. Реализуешь M-L10.3 — Shell components.

SETUP:
  cd /home/nous/paxio
  git fetch origin
  git worktree add /tmp/paxio-ml10.3-shell -b feature/M-L10.3-shell origin/dev
  cd /tmp/paxio-ml10.3-shell
  git config user.name frontend-dev
  git config user.email frontend-dev@paxio.network
  pnpm install

ЧИТАТЬ (всё, ничего больше):
  - docs/design/paxio-b5/EXTRACTED.md         (только разделы Header/Footer/Marquee)
  - docs/design/paxio-b5/Paxio-B5.html        (lines 28-90 для header, 87-90 ribbon, 102-154 footer)
  - docs/sprints/M-L10.3-shell-components.md  (этот файл)
  - packages/ui/src/index.tsx                 (текущее barrel, добавишь 3 export)
  - packages/ui/src/Footer.tsx                (текущий — рефакторишь к B5)
  - packages/ui/tests/header.test.tsx         (RED, твоя спека)
  - packages/ui/tests/footer.test.tsx         (RED)
  - packages/ui/tests/preview-ribbon.test.tsx (RED)

ПИШЕШЬ:
  - packages/ui/src/Header.tsx           (новый, 'use client')
  - packages/ui/src/Footer.tsx           (рефакторишь, breaking change OK)
  - packages/ui/src/PreviewRibbon.tsx    (новый, 'use client')
  - packages/ui/src/index.tsx            (добавляешь 3 re-export)

ПРОВЕРЯЕШЬ:
  pnpm typecheck                                — clean
  pnpm --filter @paxio/ui test                  — все GREEN, включая 3 новых
  pnpm --filter @paxio/landing-app build        — clean (landing импортирует обновлённый Footer)

КОММИТИШЬ, говоришь "M-L10.3 готов".

Cleanup: git worktree remove --force /tmp/paxio-ml10.3-shell

НЕ читать: tech-debt.md полностью, остальные CSS файлы в _design/styles/ (EXTRACTED.md
заменяет), весь Paxio-B5.html (только указанные lines).
```

## Анти-цели

- НЕ port'ить Hero/Scrolls (M-L10.4/.5)
- НЕ менять `app/layout.tsx` или `app/page.tsx` — это M-L10.5
- НЕ менять CSS tokens (это уже сделано в M-L10.2)
