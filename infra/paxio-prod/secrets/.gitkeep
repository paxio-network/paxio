# Real secret files (db_password.txt, etc.) live ONLY on the Hetzner host.
# This .gitkeep ensures the directory exists in git so Caddy + Compose
# secret-mounts find their target on first deploy. Do NOT commit *.txt.
