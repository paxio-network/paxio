# M-L10.5 — Scrolls + page wiring + M-L9 deprecation

**Тип**: Frontend implementation (frontend-dev). Финальная phase.
**Зависимости**: M-L10.2 + M-L10.3 + M-L10.4 merged.
**Worktree**: `/tmp/paxio-ml10.5-scrolls`.
**Branch**: `feature/M-L10.5-scrolls-wiring`.
**Estimated**: 3-4 hours.

## Цель

Port `PaxioLandingScrollsB5` (~1035 строк JSX) и связать всё в финальную страницу.
Удалить M-L9 секции. Установить `data-production="false"` + видимый `<PreviewRibbon/>`
для R-FE-Preview compliance.

## Готово когда

1. `apps/frontend/landing/app/sections/02-scrolls-b5.tsx` ('use client') — port
   `PaxioLandingScrollsB5` целиком. Все hardcoded numbers идут в `app/data/preview.ts`
   (расширенные exports — добавь `PREVIEW_AUDIENCES`, `PREVIEW_BITCOIN_AGENTS`,
   `PREVIEW_RADAR_INDICES`, etc., каждый с TODO маркером).
2. `apps/frontend/landing/app/page.tsx` рефакторен:
   ```tsx
   import { Header, Footer, PreviewRibbon } from '@paxio/ui';
   import { HeroB5 } from './sections/01-hero-b5';
   import { ScrollsB5 } from './sections/02-scrolls-b5';
   export default function Page() {
     return <>
       <Header/>
       <PreviewRibbon/>
       <main><HeroB5/><ScrollsB5/></main>
       <Footer year={2026}/>
     </>;
   }
   ```
3. `apps/frontend/landing/app/layout.tsx`:
   ```tsx
   <html data-theme={theme}>
     <body data-production="false" data-motion="live">
       <ThemeProvider>{children}</ThemeProvider>
     </body>
   </html>
   ```
   `data-production="false"` ОБЯЗАТЕЛЬНО per R-FE-Preview rule.
4. M-L9 sections удалены: `apps/frontend/landing/app/sections/{00-header,01-hero,02-quickstart,02b-bitcoin,03-radar,04-pay,05-network,06-doors,07-foot,preview-ribbon}.tsx` — все 10 файлов.
5. `pnpm --filter @paxio/landing-app build` clean
6. `pnpm --filter @paxio/landing-app test` GREEN
7. Visual check: open `http://localhost:3001` rendered, compare side-by-side с
   `docs/design/paxio-b5/Paxio-B5.html` opened in browser. Light + dark themes both work.

## RED test specs (architect-only)

`apps/frontend/landing/tests/page-wiring.test.tsx` (new):
- `<body data-production="false">` present
- `<PreviewRibbon/>` rendered
- `<HeroB5/>` rendered after PreviewRibbon
- `<ScrollsB5/>` rendered after HeroB5
- M-L9 secsions absent: `app/sections/01-hero.tsx` etc. NOT present (statSync throws)

`scripts/verify_M-L10.5.sh` + final `scripts/verify_M-L10-port.sh` (E2E visual fidelity).

## Промт для frontend-dev (~30 строк)

```
Ты frontend-dev. Реализуешь M-L10.5 — Scrolls + page wiring + удаление M-L9.

SETUP:
  cd /home/nous/paxio
  git fetch origin
  git worktree add /tmp/paxio-ml10.5-scrolls -b feature/M-L10.5-scrolls-wiring origin/dev
  cd /tmp/paxio-ml10.5-scrolls
  git config user.name frontend-dev
  git config user.email frontend-dev@paxio.network
  pnpm install

ЧИТАТЬ:
  - docs/design/paxio-b5/EXTRACTED.md                            (только sections audiences/bitcoin/radar/pricing/footer)
  - docs/design/paxio-b5/components/landing_scrolls_b5.jsx       (1035 строк — основной reference)
  - docs/sprints/M-L10.5-scrolls-wiring.md                       (этот файл)
  - .claude/rules/frontend-rules.md                              (R-FE-Preview)
  - apps/frontend/landing/app/page.tsx                           (текущее, перепишешь)
  - apps/frontend/landing/app/layout.tsx                         (текущее, добавишь data-production attr)
  - apps/frontend/landing/app/data/preview.ts                    (расширишь)
  - apps/frontend/landing/tests/page-wiring.test.tsx             (RED)

ПИШЕШЬ:
  - apps/frontend/landing/app/sections/02-scrolls-b5.tsx         (новый)
  - apps/frontend/landing/app/data/preview.ts                    (+ AUDIENCES, BITCOIN_AGENTS, RADAR_INDICES exports с TODO)
  - apps/frontend/landing/app/page.tsx                           (новая wiring: Header+PreviewRibbon+Hero+Scrolls+Footer)
  - apps/frontend/landing/app/layout.tsx                         (+ data-production="false")

УДАЛЯЕШЬ:
  - apps/frontend/landing/app/sections/{00-header,01-hero,02-quickstart,02b-bitcoin,03-radar,04-pay,05-network,06-doors,07-foot,preview-ribbon}.tsx  (10 файлов M-L9)

ПРОВЕРЯЕШЬ:
  pnpm typecheck
  pnpm --filter @paxio/landing-app test
  pnpm --filter @paxio/landing-app build
  pnpm --filter @paxio/landing-app dev → http://localhost:3001 → визуально совпадает с Paxio-B5.html

КОММИТИТЬ один commit. "M-L10.5 готов".

Cleanup: git worktree remove --force /tmp/paxio-ml10.5-scrolls

НЕ читай: tech-debt полностью, остальные CSS файлы _design/styles/ (EXTRACTED.md
покрывает); v_hero_b5.jsx (это уже M-L10.4).
```

## R-FE-Preview compliance final check

После M-L10.5 layout.tsx должен иметь:
- ✅ `<body data-production="false">`
- ✅ `<PreviewRibbon/>` visible сверху
- ✅ Все simulated данные в `app/data/preview.ts` с `// TODO M-L11` маркерами
- ✅ Drift-guard test pin в `tests/landing-preview-disclosure.test.ts` — это уже на dev (от M-L10 P1 как stub `it.todo`); architect напишет реальный когда M-L10.5 land'ит

## Анти-цели

- НЕ создавать новые @paxio/ui компоненты — все нужные уже есть от M-L10.3
- НЕ забывать `<PreviewRibbon/>` или `data-production="false"` — это P1 violation R-FE-Preview
- НЕ оставлять M-L9 sections как fallback — clean swap
