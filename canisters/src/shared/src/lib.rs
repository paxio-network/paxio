//! Paxio shared types — common Rust types used across canisters.
//!
//! Mirrors (where semantically applicable) TypeScript types in `app/types/`.
//! Real content added in M01 when bitgent canisters/shared/types.rs is ported.

// Placeholder — will be populated in M01 when bitgent canisters are ported.
// Target: Did, AgentCard, Capability, Result<T,E> equivalents.

pub const VERSION: &str = "0.1.0";
